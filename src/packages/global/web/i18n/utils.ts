/**
 * i18n 工具函数简化版
 * 本项目是后端 API 服务，不需要完整的 i18n 翻译功能
 * 此处提供一个简化版本，直接返回 key 作为占位符
 */

/**
 * 国际化翻译函数
 * 在后端服务中，我们直接返回 key 作为标识符
 * 前端会根据自己的 i18n 配置进行翻译
 */
export const i18nT = (key: string): string => key;
